syntax = "proto3";

package phonebook;

// Always import protos with a full path relative to the WORKSPACE file.
import "protobuf/phonebook/zip_code.proto";

message Address {
  string city = 1;
  ZipCode zip_code = 2;
}
